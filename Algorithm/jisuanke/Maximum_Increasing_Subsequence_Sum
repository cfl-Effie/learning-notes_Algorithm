/*
一个数的序列bi,当b1<b2 <…< bs的时候,我们称这个序列是上升的。
蒜头君给定了一个序列(a1,a2,…,aN),我们可以得到一些上升的子序列(ai1,ai2,…,aik),这里1≤i1<i2 <….< iK≤N。
比如,对于序列(1,7,3,5,9,4,8),有它的一些上升子序列,如(1,7),(3,4,8)等等。这些子序列中序列和最大为18,为子序列(1,3,5,9)的和。
你的任务,就是对于给定的序列,求出最大上升子序列和。
注意,最长的上升子序列的和不一定是最大的,比如序列(100,1,2,3)的最大上升子序列和为100,而最长上升子序列为(1,2,3)。
输入格式
  输入的第一行是序列的长度N(1≤N≤1000)。
  第二行给出序列中的N个整数,这些整数的取值范围都在0到10000(可能重复)。
输出格式
  最大上升子序列和。
eg:
  in:
    7
    1 7 3 5 9 4 8
  out:18
*/
#include <bits/stdc++.h>
using namespace std;
int main() {
   int n;
   cin >> n;
   vector<int> a(n), dp(n);
   // 输入数组
   for (int i = 0; i < n; ++i) {
       cin >> a[i];
       dp[i] = a[i]; // 初始化 dp[i]
   }
   int maxSum = 0;
   // 动态规划求解
   for (int i = 0; i < n; ++i) {
       for (int j = 0; j < i; ++j) {
           if (a[i] > a[j]) {
               dp[i] = max(dp[i], dp[j] + a[i]);
           }
       }
       maxSum = max(maxSum, dp[i]); // 更新最大值
   }
   cout << maxSum << endl;
   return 0;
}
